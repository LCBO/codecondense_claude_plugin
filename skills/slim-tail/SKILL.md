---
name: slim-tail
description: Show recent condense telemetry events (last 30 by default). Pass a number arg to change.
---

Run `node ~/.slimmer/plugin/cli/slim.js tail [N]` (or `slim tail N`) and print the events.

Grok form of `/slim-tail`.
