---
name: slim-reset
description: Clear all condense telemetry events (lifetime savings reset). Irreversible.
---

**Warning:** this deletes the local event log used for savings reports.

Run `node ~/.slimmer/plugin/cli/slim.js reset` (or `slim reset`) only when the user explicitly confirms.

Grok equivalent of `/slim-reset`.
