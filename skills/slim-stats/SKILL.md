---
name: slim-stats
description: Show condense code-index stats (files/symbols counts) for current project.
---

Run `node ~/.slimmer/plugin/cli/slim.js stats` (or `slim stats`) and relay the counts and index path.

Grok equivalent of `/slim-stats`.
