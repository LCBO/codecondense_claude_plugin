---
name: slim-status
description: Show condense licensing / trial status — registered, plan, calls used/remaining.
allowed-tools: Bash(node *)
---

```bash
node --no-warnings=ExperimentalWarning ${CLAUDE_PLUGIN_ROOT}/cli/slim.js status
```

Relay full output verbatim.
