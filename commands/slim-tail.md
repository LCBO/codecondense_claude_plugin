---
name: slim-tail
description: Show recent condense telemetry events (last 30 by default). Pass a number arg to change.
allowed-tools: Bash(node *)
---

```bash
node --no-warnings=ExperimentalWarning ${CLAUDE_PLUGIN_ROOT}/cli/slim.js tail $ARGUMENTS
```

Relay full output verbatim.
