---
name: slim-savings
description: Show condense plugin savings — tool calls, tokens, cost (session + lifetime).
allowed-tools: Bash(node *)
---

```bash
node --no-warnings=ExperimentalWarning ${CLAUDE_PLUGIN_ROOT}/cli/slim.js savings
```

Relay full output verbatim.
